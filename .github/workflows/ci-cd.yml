# .github/workflows/deploy.yml
name: CI/CD

on:
  push:
    branches:
      - stage
  workflow_dispatch:  # позволяет запустить вручную из GitHub UI

concurrency:
  group: ci-cd-stage
  cancel-in-progress: true  # отменяем старый деплой если пришёл новый

jobs:
  # ──────────────────────────────────────────────────────────
  # JOB 1: Тесты
  # ──────────────────────────────────────────────────────────
  test:
    name: 🧪 Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Poetry
        run: pip install poetry==1.8.2

      - name: Cache Poetry dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/pypoetry
          key: poetry-${{ hashFiles('poetry.lock') }}

      - name: Install dependencies
        run: poetry install --with dev

      - name: Run tests
        run: poetry run pytest -v --tb=short
        env:
          ENVIRONMENT: testing
          BOT_TOKEN: test_token
          POSTGRES_HOST: localhost
          POSTGRES_DB: test_db
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: test_pass
          REDIS_HOST: localhost
          REDIS_PORT: 6379
          ADMIN_API_KEY: test_key

  # ──────────────────────────────────────────────────────────
  # JOB 2: Билд и пуш образа в Docker Hub
  # ──────────────────────────────────────────────────────────
  build-and-push:
    name: 🐳 Build & Push Image
    runs-on: ubuntu-latest
    needs: test  # запускается только если тесты прошли
    environment: production

    outputs:
      # Передаём тег образа в следующий job
      app_image: ${{ steps.vars.outputs.app_image }}
      app_tag: ${{ steps.vars.outputs.app_tag }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set image variables
        id: vars
        env:
          REGISTRY_USERNAME: ${{ secrets.REGISTRY_USERNAME }}
        run: |
          USERNAME_LOWER=$(echo "$REGISTRY_USERNAME" | tr '[:upper:]' '[:lower:]')
          # Образ: docker.io/yourusername/genetic-wordle
          echo "app_image=docker.io/${USERNAME_LOWER}/genetic-wordle" >> "$GITHUB_OUTPUT"
          # Тег: sha коммита для точной версии + latest для удобства
          echo "app_tag=app-${GITHUB_SHA}" >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          registry: docker.io
          username: ${{ secrets.REGISTRY_USERNAME }}
          password: ${{ secrets.REGISTRY_PASSWORD }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ci-cd-files/Dockerfile
          target: production
          push: true
          # Два тега: точный (sha) и latest
          tags: |
            ${{ steps.vars.outputs.app_image }}:${{ steps.vars.outputs.app_tag }}
            ${{ steps.vars.outputs.app_image }}:latest
          # Кэширование слоёв — ускоряет повторные билды
          cache-from: type=registry,ref=${{ steps.vars.outputs.app_image }}:buildcache
          cache-to: type=registry,ref=${{ steps.vars.outputs.app_image }}:buildcache,mode=max

  # ──────────────────────────────────────────────────────────
  # JOB 3: Деплой на сервер
  # ──────────────────────────────────────────────────────────
  deploy:
    name: 🚀 Deploy
    runs-on: ubuntu-latest
    needs: build-and-push
    environment: production

    steps:
      - name: Validate secrets
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USER: ${{ secrets.SSH_USER }}
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          APP_DIR: ${{ secrets.APP_DIR }}
          REGISTRY_USERNAME: ${{ secrets.REGISTRY_USERNAME }}
          REGISTRY_PASSWORD: ${{ secrets.REGISTRY_PASSWORD }}
        run: |
          set -eu
          missing=""
          for name in SSH_HOST SSH_USER SSH_PRIVATE_KEY APP_DIR REGISTRY_USERNAME REGISTRY_PASSWORD; do
            eval "value=\${$name}"
            if [ -z "$value" ]; then
              missing="$missing $name"
            fi
          done
          if [ -n "$missing" ]; then
            echo "❌ Missing secrets:$missing"
            exit 1
          fi
          echo "✅ All secrets present"

      - name: Deploy over SSH
        uses: appleboy/ssh-action@v1.2.0
        env:
          REGISTRY: docker.io
          REGISTRY_USERNAME: ${{ secrets.REGISTRY_USERNAME }}
          REGISTRY_PASSWORD: ${{ secrets.REGISTRY_PASSWORD }}
          APP_IMAGE: ${{ needs.build-and-push.outputs.app_image }}
          APP_TAG: ${{ needs.build-and-push.outputs.app_tag }}
          DEPLOY_SHA: ${{ github.sha }}
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          script_stop: true
          envs: REGISTRY,REGISTRY_USERNAME,REGISTRY_PASSWORD,APP_IMAGE,APP_TAG,DEPLOY_SHA
          script: |
            set -eu
            APP_DIR="${{ secrets.APP_DIR }}"
            cd "$APP_DIR"

            echo "📥 Pulling latest code..."
            git fetch --all --prune
            git checkout stage
            git reset --hard origin/stage

            echo "🔐 Logging into Docker Hub..."
            echo "$REGISTRY_PASSWORD" | docker login "$REGISTRY" \
              -u "$REGISTRY_USERNAME" --password-stdin

            echo "📦 Pulling new image: ${APP_IMAGE}:${APP_TAG}"
            APP_IMAGE="$APP_IMAGE" APP_TAG="$APP_TAG" \
              docker compose -f docker-compose.registry.yml pull api

            echo "🗄️ Starting postgres and redis..."
            # APP_IMAGE и APP_TAG передаём чтобы не было warnings
            APP_IMAGE="$APP_IMAGE" APP_TAG="$APP_TAG" \
              docker compose -f docker-compose.registry.yml up -d postgres redis

            echo "⏳ Waiting for postgres to be healthy..."
            timeout 60 sh -c '
              until docker inspect genetic-wordle-postgres \
                --format="{{.State.Health.Status}}" | grep -q "healthy"; do
                echo "  waiting..."
                sleep 3
              done
            '

            echo "🔄 Restarting api container..."
            APP_IMAGE="$APP_IMAGE" APP_TAG="$APP_TAG" \
              docker compose -f docker-compose.registry.yml up -d --remove-orphans api

            echo "🧬 Running DB init (skips if already exists)..."
            APP_IMAGE="$APP_IMAGE" APP_TAG="$APP_TAG" \
              docker compose -f docker-compose.registry.yml exec -T api \
              python scripts/init_genes.py || echo "⚠️ Init skipped or already done"

            echo "🧹 Cleaning old images..."
            docker image prune -f

            echo "📊 Container status:"
            APP_IMAGE="$APP_IMAGE" APP_TAG="$APP_TAG" \
              docker compose -f docker-compose.registry.yml ps

            echo "✅ Deploy complete! SHA: ${DEPLOY_SHA}"